# ── Private data (never shared between instances) ──
#
# BLUEPRINT TEMPLATE: context/ is committed (example/template content).
# CLIENT FORKS: After initial setup, uncomment the line below to stop
# tracking client-specific context changes:
# context/
data/captures/
data/outputs/
data/reports/
# Test CSVs contain real contact details + guessed emails for per-deployment
# MVP portability tests. Ephemeral — don't track in git.
data/test_contacts_*.csv
.env

# ── Python ──
__pycache__/
*.pyc
.venv/
*.egg-info/

# ── OS ──
.DS_Store
Thumbs.db

# ── IDE ──
.vscode/
.idea/

# ── Local logs (cron output, etc.) ──
logs/
*.log
