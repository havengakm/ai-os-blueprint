# ── Private data (never shared between instances) ──
# NOTE: context/ IS committed in the blueprint template repo.
# In client forks, add "context/" to .gitignore after initial setup.
data/captures/
data/outputs/
.env

# ── Python ──
__pycache__/
*.pyc
.venv/
*.egg-info/

# ── OS ──
.DS_Store
Thumbs.db

# ── IDE ──
.vscode/
.idea/
