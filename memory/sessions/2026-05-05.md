# Session 2026-05-05

## Summary
Brainstorming-in-progress: reorganising AIOS folder structure, splitting context across two new Obsidian vaults (Climb Co Brain = business, Cursed Brain = personal), and decomposing the monolith repo into smaller projects with cleaner skills placement. No artefact changes this turn — only repo exploration and a clarifying question to operator.

## Decisions Made
None yet. Awaiting operator answer to Question 1: which model for AIOS↔Obsidian-vault interaction (A=read-only-for-human, B=AIOS-reads-vault-paths, C=sync-vault-into-repo).

## Files Updated
- `memory/sessions/2026-05-05.md` — created (this session log)

## Action Items for Next Session
- [ ] Operator to answer Question 1 (vault interaction model)
- [ ] Continue brainstorming flow: more clarifying questions → 2-3 approaches → design → spec doc at `docs/superpowers/specs/2026-05-05-aios-folder-reorg-design.md`
- [ ] Identify what content moves to Climb Co Brain vs Cursed Brain vs stays in repo
- [ ] Plan skills folder cleanup (orphans like `skills/cold-email-body-writer/` → nest under category)
- [ ] Decide if AIOS becomes one repo or multiple (foundation / Scout / Beacon / Optimizer)

## Counts
- Files changed: 1 (this session log)
- Commits: 0
- Tests: not run

---

## Update (Q1 answered)

### Decisions Made
1. **Vault interaction model = B for both vaults.** Climb Co Brain (business) AND Cursed/Kirst Brain (personal) both treated as source-of-truth that AIOS reads via configured vault paths. Rationale: personal-brand context may feed Clymb Co deployment in some instances, so AIOS needs read access to both, not just business.
2. **Naming TBC.** Operator wrote "Cursed Brain" initially and "Kirst Brain" in the follow-up. Confirm canonical name before any path config is written.

### Action Items
- [ ] Operator to answer Question 2 (repo granularity: A=2 repos, B=many, C=monorepo cleanup)
- [ ] Confirm canonical name for personal vault (Cursed Brain vs Kirst Brain)
- [ ] Get on-disk paths for both vaults (needed for AIOS config under model B)

---

## Update (Q2 answered + topology proposal on the table)

### Decisions Made
3. **Multi-repo, driven by ephemeral-routine cloning.** Operator confirmed: routines clone a repo to run, then the clone is deleted; trigger.dev jobs likewise need scoped clones. Each routine should pull only the slice (skills + context) it actually uses — Scout-sized for list-building, Content-sized for LinkedIn, etc. Not "premature decomposition" — it's required by the runtime model.
4. **Hard constraint = shared foundation.** Independent repos must still interact, learn, and optimise from each other via the AIOS foundation. "Lego blocks on a foundation." Decisions/findings flow into a shared data context for continuous evolution.

### Topology proposed (awaiting confirmation)
- `aios-foundation` (Python package) — autonomy, decision logger, memory, knowledge embedder, registry, feedback loop, Supabase client
- `aios-scout` / `aios-beacon` / `aios-optimizer` / `aios-content` — system repos, each imports `aios-foundation`, carries only its own skills
- `clymb-co-deployment` — per-deployment config + routine schedules, composes which systems run
- `clymb-co-brain` (Obsidian vault as git repo) — business knowledge
- `kirst-brain` (Obsidian vault as git repo) — personal + expert reference

Shared substrate = Supabase (decisions/memory/learnings tables). Foundation pkg = shared primitives. Together they give every system identical code AND identical data.

### Action Items
- [ ] Operator to confirm topology matches mental model
- [ ] Then Q3: foundation distribution mechanism (pip-from-git-tag vs private registry vs submodule)

---

## Update (Plan approved)

### Decisions Made
5. **Topology + data-plane placement + productisation approved.** Final plan saved to `/home/kirsten/.claude/plans/yes-this-fits-with-moonlit-elephant.md` and approved via ExitPlanMode.
6. **CRM = Attio.** Sync direction: AIOS pushes positive-intent contacts; Attio pushes deal-stage updates back to `outcomes` table.
7. **ClickUp deferred.** Operator has prior experience but not yet adopted in current business. Document placement; layer in only when human-side review pain forces it.
8. **Personal vault canonical name = Kirst Brain.**
9. **Email split = Gmail (operator inbox) + Zoho (cold-email sending domains).** Don't unify; deliverability best practice.
10. **Calendar/Drive deferred.** Default to Google Calendar + Cal.com when activated. Drive minimal use (binary assets that don't fit git or Obsidian).
11. **Productisation is first-class.** Three template repos (`aios-deployment-template`, `aios-business-brain-template`, `aios-personal-brain-template`) instantiate per-deployment instances. Phase 5 = provision `acme-co` test client end-to-end via SOP only (zero custom code) as the productisation gate.
12. **Foundation distribution = pip install from git tag.** No package registry needed; tagged git installs are first-class pip and survive ephemeral clones.

### Files Updated
- `/home/kirsten/.claude/plans/yes-this-fits-with-moonlit-elephant.md` — comprehensive design (8 sections + open items)
- `memory/sessions/2026-05-05.md` — this delta

### Action Items for Next Session
- [ ] Operator to confirm GitHub org/owner for new repos (personal account vs new `<name>-aios` org)
- [ ] Operator to confirm vault on-disk paths
- [ ] Operator to confirm vault hosting (private GitHub recommended)
- [ ] Invoke `writing-plans` for detailed Phase 1 (foundation extraction) execution plan before any repo creation
- [ ] After Phase 1 plan approved: execute it (create `aios-foundation` repo, copy modules, tag v0.1.0, swap monorepo imports, verify tests)

### Counts
- Files changed this session: 2 (plan file + session log)
- Commits: 0
- Tests: not run

---

## Update (Phase 1 prep — pending operator answers)

### Decisions Made
13. **GitHub repos go in a new org** (operator-confirmed via AskUserQuestion). Specific org name still needed before any `gh repo create` runs.
14. **Phase 1 will get a detailed execution plan first** (writing-plans skill) before any code/repo movement. Plan file destination proposed: `docs/superpowers/plans/2026-05-05-phase1-foundation-extraction.md`.

### Action Items
- [ ] Operator to provide actual org name (suggestions surfaced: `aios-kit`, `aiosos`, `kirsten-aios`, etc.)
- [ ] Confirm plan file path (`docs/superpowers/plans/`)
- [ ] Then: invoke writing-plans for Phase 1 detail
- [ ] Then: execute Phase 1 (foundation extraction)

---

## Update (Phase 1 detailed execution plan written)

### Decisions Made
15. **GitHub org = `aios-kit`.** New org for the productised AIOS kit (brand-neutral, not tied to Clymb Co).
16. **Phase 1 plan saved** to `docs/superpowers/plans/2026-05-05-phase1-foundation-extraction.md` (location is interim until folder reorg moves docs/).
17. **Critical scoping discovery:** `aios/foundation/registry.py` imports from `systems.scout.supabase_backends`, so it's NOT pure foundation. Excluded from Phase 1; moves to deployment template (Phase 3). Same for top-level `aios/registry.py` (depends on `systems.base`) — defer to Phase 2.
18. **Namespace strategy = PEP 420.** Delete monorepo's `aios/__init__.py` so the pip-installed `aios.foundation.*` and `aios.memory.*` from the new package coexist with monorepo's remaining `aios.daemon.*`. Cleanest path; avoids any import rewrites in calling code.
19. **Eight pure modules migrate to `aios-foundation`:** `autonomy`, `decision_logger`, `embedder`, `employee_memory`, `feedback_loop`, `knowledge`, `pattern_matcher` (all from `aios/foundation/`) + `aios/memory/store.py`. Plus cross-cutting skills (`skills/meta/`, `skills/playbooks/`) and `rules/global-writing-guardrails.md`.

### Files Updated
- `docs/superpowers/plans/2026-05-05-phase1-foundation-extraction.md` — created (11 tasks, ~50 steps, every step grounded in actual code paths and commands)
- `memory/sessions/2026-05-05.md` — this delta

### Action Items for Next Session
- [ ] Operator picks execution approach (subagent-driven vs inline)
- [ ] Execute Phase 1 per plan
- [ ] After Phase 1 cutover green: 24h soak in production, then Phase 2 (carve `aios-scout`)

### Counts (Phase 1 plan)
- Tasks: 11
- Steps total: ~50
- Files to be created in new repo: ~25
- Files to be deleted from monorepo: ~20 (foundation modules + memory + cross-cutting skills + tests)

---

## Update (Subagent execution mode + pre-Phase-1 checkpoint committed)

### Decisions Made
20. **Execution mode = subagent-driven-development.** Fresh implementer per task + two-stage review (spec compliance, then code quality). Skill loaded; prompt templates inspected.
21. **GitHub strategy = new org `aios-kit`** (re-confirmed after operator clarified that an org is just a namespace under existing account, not a new login). Manual prereqs surfaced: org creation via web UI + `gh auth refresh -h github.com -s admin:org`.
22. **Pre-Phase-1 checkpoint committed to main** (commit `e9e700a`). Captures all prior-session accumulated work: retired old superpowers decisions/plans/specs, updated CLAUDE.md/MEMORY.md/INDEX.md, new session logs, new READMEs, new skills, new scripts. Clean rollback target before the migration branch.

### Files Updated (this delta)
- `memory/sessions/2026-05-05.md` — this delta
- 30 files committed in checkpoint `e9e700a` (3210 insertions, 10011 deletions)

### Action Items for Next Session
- [ ] **OPERATOR (manual):** Create `aios-kit` GitHub org via https://github.com/account/organizations/new (free plan, name `aios-kit`)
- [ ] **OPERATOR (manual):** Run `gh auth refresh -h github.com -s admin:org` to add admin:org scope to gh CLI token
- [ ] **CONTROLLER:** Verify both prereqs (`gh api orgs/aios-kit` returns 200, `gh auth status` shows `admin:org` scope)
- [ ] **CONTROLLER:** Dispatch implementer subagent for Phase 1 Task 1 (bootstrap aios-foundation skeleton)

### Counts
- Files changed this session: 32 (30 in checkpoint commit + 2 plan/log files counted above)
- Commits this session: 1 (`e9e700a` checkpoint)
- Tests run: 0 (no test changes yet — Task 1 onwards)

---

## Update (Phase 1 Tasks 1-7 COMPLETE — `aios-foundation` v0.1.0 published)

### Decisions Made
23. **Foundation v0.1.0 released.** Tag `v0.1.0` lives at https://github.com/aios-kit/aios-foundation/releases/tag/v0.1.0. Tag SHA `60b4eda` points at commit `1b3b4ac`. Pip install from tag verified end-to-end (clean venv + downstream pyproject pin both green).
24. **Trigify playbooks moved out of foundation scope.** Code-quality review for Task 4 surfaced that `configure-trigify-monitors.md` and `discover-trigify-leads.md` are Scout-specific (not foundation-cross-cutting). They stay in monorepo until Phase 2 → `aios-scout`. Plan file updated to reflect.
25. **Cloudflare playbook generic-ified.** 5 monorepo-specific references (clutch.py paths + slice/date markers) replaced with deployment-agnostic placeholders. Pattern still useful; example specifics removed.
26. **PEP 735 dep-groups + correct uv command.** `uv sync --group dev` is the install command (not `uv pip install -e ".[dev]"` which silently skips dev deps with PEP 735 groups). Plan file updated; CI workflow uses correct command. CI green on first run (13s).
27. **Two forward-looking notes for Task 8:** (a) HTTPS-vs-SSH auth — operator's machine is SSH-only, global git rewrite now in place; (b) hatchling-specific pyproject quirks would matter ONLY if monorepo was a hatchling-built package, which it isn't. Both are environmental, not blockers.

### Foundation repo final state (commits in chronological order)
- `44f06db` chore: bootstrap aios-foundation repo skeleton
- `cc63bfb` chore: gitignore uv.lock + add .gitkeep for empty scaffold dirs
- `5cad1a1` feat: migrate seven aios.foundation modules + tests
- `a5fba37` feat: migrate aios.memory.store + test
- `45bb126` chore: migrate cross-cutting skills + writing guardrails
- `7da4ed1` fix: generic-ify Cloudflare playbook + remove Scout-specific Trigify playbooks
- `6d781ef` docs: add RUNBOOK and public API surface doc
- `1b3b4ac` ci: add tests workflow
- TAG `v0.1.0` → `1b3b4ac`

### Files Updated (this delta)
- `~/01_PERSONAL/10_PERSONAL_PROJECTS/aios-foundation/` — full new repo, 8 commits, v0.1.0 release
- `docs/superpowers/plans/2026-05-05-phase1-foundation-extraction.md` — refined Task 4 scope (Trigify playbooks excluded), Task 8 deletion list updated, install command fixed to `uv sync --group dev`

### Action Items for Next Session
- [ ] **PAUSE GATE:** Operator confirms before Task 8 (high-risk monorepo cutover that adds aios-foundation pip dep + deletes the migrated modules from the monorepo)
- [ ] Task 8: monorepo cutover on feature branch `feat/foundation-extraction`
- [ ] Task 9: run full monorepo test suite (must remain green minus the 81 deleted tests)
- [ ] Task 10: daemon smoke test (`scripts/run_daemon_once.py --dry-run`)
- [ ] Task 11: final commit + PR

### Counts (Tasks 1-7)
- Subagent dispatches: 12 (5 implementers, 4 spec reviewers, 2 code-quality reviewers, 1 fix-implementer)
- Aios-foundation commits: 8
- Aios-foundation lines: ~115 (RUNBOOK + public-api.md, plus all migrated content)
- Tests passing in aios-foundation: 81/81
- CI runs: 1 (green, 13s)
- Plan file edits: 4 (install cmd fix x2, Task 4 scope refinement, Task 8 cleanup-list refinement)

